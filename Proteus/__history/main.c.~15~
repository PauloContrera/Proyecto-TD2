#include <main.h>
int d[5];
void main(){
  SET_TRIS_C(0xff0);
   SET_TRIS_B(0X00f);
   SET_TRIS_A(00000000);
   PORTB=0;
   PORTA=0;
   delay_ms(10);
   int d[4];
   while(TRUE)
   
   {
   //numerico=numerico+0.03;
   //convertirdato(numerico);
   //muestra(dato);
   
   for (int i=0;i<5;i++) {
      n[i]=bit_test(PORTB,i);
   }
   if(n[3]==1){
   numerico=numerico+0.03;
   convertirdato(numerico);
   muestra(dato);
   }
   }

}



