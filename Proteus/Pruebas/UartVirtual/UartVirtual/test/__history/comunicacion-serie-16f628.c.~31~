#include <18f2550.h>
#use delay(clock=400000)
#use rs232(baud=300, xmit=PIN_c7,rcv=PIN_c6,stream=PiC)

void main()
{
 for(;;)
 {
 
fprintf(PiC,"Digite lo que desea mandar al Segundo PIC: \r\n");

delay_ms(1000);

 }
}
